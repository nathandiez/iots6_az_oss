datadog:
  datadog:
    apiKeyExistingSecret: datadog-secret
    site: us5.datadoghq.com
    clusterName: iots6-aks
    logs:
      enabled: true
      containerCollectAll: true
    processAgent:
      processCollection: true
    kubeStateMetricsCore:
      enabled: true
  clusterAgent:
    enabled: true
    replicas: 2
